# Implementation Plans

Current follow-up generated by the improve execution/review loop on 2026-07-23.
Historical numbering continues after plans 001–014, which exist in repository history but are not present at current `main`.

## Execution order and status

| Plan | Title                                                           | Priority | Effort | Depends on                              | Status  |
| ---- | --------------------------------------------------------------- | -------- | ------ | --------------------------------------- | ------- |
| 015  | Make legacy credential migration fail safe before Dexie cleanup | P1       | M      | Maintainer credential-recovery decision | BLOCKED |

## Current implementation artifact

- Isolated worktree: `/Users/allangarcez/orca/workspaces/writeme-editor/db-refactor-review`
- Branch: `g4rcez/db-refactor-review`
- Commit: `54057af6409e28a1106bc147387ca89dedaafe3e`
- Verdict: **BLOCK** — do not merge yet. The final reviewer found that inherited v2 `verified` state can bypass the stricter credential policy, and that undecryptable differing destination values are ambiguous ciphertext/plaintext.
- The user’s `main` branch has not received source changes from this execution.

## Dependency notes

Plan 015 cannot execute until the maintainer chooses the behavior for an undecryptable SQLite credential that differs from the Dexie source. Recommended: leave both stores unchanged, return `skipped`, retain Dexie, and require explicit credential re-entry rather than guessing or overwriting.

## Findings considered and rejected

- **Unstable UUIDv7 for ID-less Dexie migration rows:** rejected. Current Dexie stores use required inline primary keys; a valid ID-less source row cannot exist. Malformed non-string IDs now fail validation and retain source data.
- **Route global recent notes through the specialized query:** rejected for this refactor. It changes behavior for legacy records with missing `noteType` and exposes an existing Windows path-separator mismatch.
- **Hydrate every generic SQLite date in the storage adapter:** rejected. Note repositories parse entities before date sorting, and terminal-session sorting already handles string dates; no affected generic caller was found.
