# <local-command-caveat>Caveat: The messages below were generated by the user whil

_Session: 7c870c4f-fb30-400b-8745-194c1c89e072 | Saved: 2026-05-03 15:30:01_

## Prompt

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

## Plan

Good, I have full context. The issue is clear: `round(1 month in days)` fails because mathjs's `round` doesn't accept a `Unit` — `1 month in days` evaluates to a `Unit` object (30.4375 days), not a number.

**Fix**: preprocessor that rewrites `round(<expr> in <unit>)` → `round(number(<expr>, '<unit>'))`. mathjs's `number(unit, targetUnit)` strips the unit and returns a scalar, making it valid input for `round`.

## Resolution

Now add tests:
